function Canada(props) {
    return (
        <div>
            <h1>{props.name}</h1>
            <p>{props.p} posts</p>
            <p>{props.f1} followers</p>
            <p>{props.f2} following</p>
        </div>
    )
    
}

export default Canada;
